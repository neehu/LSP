// import { AbstractControl, ValidationErrors,AsyncValidator } from '@angular/forms'
// import { LetsShopService } from '../Services/lets-shop.service';
// import { Observable } from "rxjs/Observable";
// import 'rxjs/add/operator/map';


  
// function UserNameValidator (letsShopService:LetsShopService):AsyncValidator
// {
    
//   return (control: AbstractControl): Promise<ValidationErrors | null> | Observable<ValidationErrors | null> => {
//     return userService.getUserByMobileNumber(control.value).map(
//       users => {
//         return (users && users.length > 0) ? {"mobNumExists": true} : null;
//       }
//   }
// }
